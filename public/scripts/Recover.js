'use strict' // enables ES6 syntax for classes


class Recover extends UtilityButton {

	constructor(route) {
		super("recover", route); // initializes the base class or super class, this is the base class initializer syntax
	}
}